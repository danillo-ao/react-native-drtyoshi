import {Theme} from '@theme/theme.type';

import {
  colors,
  fonts,
  fontsSizes,
  fontsWeights,
  unit
} from '@theme/theme.values';

const theme: Theme = {
  colors,

  fonts,
  fontsSizes,
  fontsWeights,

  unit,
};

export default theme;
